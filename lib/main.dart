import 'package:flutter/material.dart';

main() {
  runApp(const Center(
    child: Text(
      "OLá",
      textDirection: TextDirection.ltr,
      style: TextStyle(
        fontSize: 40.0,
        color: Colors.blueAccent,
      ),
    ),
  ));
}
